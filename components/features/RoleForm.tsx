'use client';

import { useState } from 'react'; import { useRouter } from 'next/navigation'; import { zodResolver } from '@hookform/resolvers/zod'; import { useForm } from 'react-hook-form'; import { z } from 'zod'; import Button from '@/components/ui/Button'; import { api } from '@/services/api';
const permissions = ['Manage users', 'Manage roles', 'Manage projects', 'Manage tickets', 'Manage clients', 'Manage resources', 'View reports', 'Configure settings'];
const schema = z.object({ name: z.string().min(3), description: z.string().min(10), permissions: z.array(z.string()).min(1, 'Select at least one permission') });
type Values = z.infer<typeof schema>;
export default function RoleForm() { const router = useRouter(); const [error, setError] = useState(''); const { register, handleSubmit, formState: { errors, isSubmitting } } = useForm<Values>({ resolver: zodResolver(schema), defaultValues: { name: '', description: '', permissions: [] } }); const submit = handleSubmit(async (values) => { try { setError(''); await api.submit('roles', values); router.push('/admin/roles'); } catch (reason) { setError(reason instanceof Error ? reason.message : 'Unable to create role.'); } }); return <form onSubmit={submit} className="card space-y-6 p-6"><label><span className="label">Role name</span><input {...register('name')} className="field" placeholder="Delivery lead" /><span className="error-text">{errors.name?.message}</span></label><label><span className="label">Description</span><textarea {...register('description')} className="field" rows={4} /><span className="error-text">{errors.description?.message}</span></label><fieldset><legend className="label mb-3">Permissions</legend><div className="grid gap-3 sm:grid-cols-2">{permissions.map((permission) => <label key={permission} className="flex items-center gap-3 rounded-xl border border-slate-200 p-4 hover:bg-slate-50"><input {...register('permissions')} value={permission} type="checkbox" className="checkbox" /><span className="text-sm font-semibold text-slate-700">{permission}</span></label>)}</div><span className="error-text">{errors.permissions?.message}</span></fieldset>{error && <p role="alert" className="rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</p>}<div className="flex justify-end"><Button type="submit" disabled={isSubmitting}>{isSubmitting ? 'Creating…' : 'Create role'}</Button></div></form>; }
